import React from 'react';
import ReactDOM from 'react-dom/client';
import Generador from './components/GeneradorContra';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Generador />
  </React.StrictMode>
);


